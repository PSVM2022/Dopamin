<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--&lt;!&ndash;namespace는 이름을 구별하기 위한 공간입니다. 쉽게 얘기하면 id는 구별되어야 하므로 별도의 그c룹으로 나눕니다.-->
<!--그래야 id의 충돌하는 경우가 적어지니까. 다른 이유도 있는데 일단 이 정도만 알아두시기 바랍니다.&ndash;&gt;-->
<mapper namespace="com.PSVM.dopamin.dao.MyPageMapper">
    <select id="selectrevw" parameterType="String" resultType="RevwDto">
        select r.user_id, r.revw_id, r.cnts_id, r.revw_body, c.cnts_title
        from REVW r
                 join CNTS c on c.cnts_id = r.cnts_id
        where r.user_id = #{userId}
    </select>

    <select id="selectrevwEval" parameterType="String" resultType="RevwDto">
        select re.revw_id, re.user_id, re.revw_eval, c.cnts_title, r.revw_visib_stat
        from REVW_EVAL re
                 join REVW r on r.revw_id = re.revw_id
                 join CNTS c on c.cnts_id = r.cnts_id
        where re.user_id = #{user_id}
          AND r.revw_visib_stat = 1;
    </select>

    <select id="selectCntsWish" parameterType="String" resultType="MyPageCntsDto">
        select cw.cnts_id, cw.user_id, cw.in_date, c.cnts_postr_img, c.cnts_visib_stat
        from CNTS_WISH cw
                 join CNTS c on c.cnts_id = cw.cnts_id
        where cw.user_id = #{user_id}
          and c.cnts_visib_stat = 1;
    </select>

    <select id="selectCntsView" parameterType="String" resultType="MyPageCntsDto">
        select cv.cnts_id, cv.user_id, cv.in_date, c.cnts_postr_img, c.cnts_visib_stat
        from CNTS_VIEW cv
                 join CNTS c on c.cnts_id = cv.cnts_id
        where cv.user_id = #{user_id}
          and c.cnts_visib_stat = 1;
    </select>

    <select id="selectPost" parameterType="String" resultType="MyPagePostDto">
        select user_id,
               bbs_id,
               post_id,
               post_title,
               post_view_cnt,
               post_reply_cnt,
               in_date,
               post_visib
        from POST
        order by in_date desc;
    </select>

</mapper>


        <!--&lt;!&ndash;    전체 게시물 걋수 가져오기&ndash;&gt;-->
        <!--    <select id="count" resultType="int">-->
        <!--        SELECT count(*)-->
        <!--        FROM board-->
        <!--    </select>-->

        <!--    <delete id="deleteAll">-->
        <!--        DELETE-->
        <!--        FROM board-->
        <!--    </delete>-->

        <!--&lt;!&ndash;    삭제할 때는 게시물 번호하고 작성자하고 일치해야 삭제 가능 &ndash;&gt;-->
        <!--    <delete id="delete" parameterType="map">-->
        <!--        DELETE-->
        <!--        FROM board-->
        <!--        WHERE bno = #{bno}-->
        <!--          and writer = #{writer}-->
        <!--    </delete>-->

        <!--&lt;!&ndash;    매개변수로 BoardDto를 주면  #{}이걸 채워주는것 &ndash;&gt;-->
        <!--    <insert id="insert" parameterType="BoardDto">-->
        <!--        INSERT INTO board-->
        <!--            (title, content, writer)-->
        <!--        VALUES (#{title}, #{content}, #{writer})-->
        <!--    </insert>-->

        <!--    <select id="selectAll" resultType="BoardDto">-->
        <!--        SELECT bno, title, content, writer, view_cnt, comment_cnt, reg_date-->
        <!--        FROM board-->
        <!--        ORDER BY reg_date DESC, bno DESC-->
        <!--    </select>-->

        <!--    <sql id="selectFromBoard">-->
        <!--        SELECT bno, title, content, writer, view_cnt, comment_cnt, reg_date-->
        <!--        FROM board-->
        <!--    </sql>-->

        <!--&lt;!&ndash;    게시물 읽을 때 게시물 번호에 해당하는걸 읽어와야 하니깐  WHERE bno = #{bno} 조건 주고 parameterType="int" 파라미터 타입을 int로 줬다. &ndash;&gt;-->
        <!--&lt;!&ndash; resultType="BoardDto" 원래는 리절트 타입을 줄줄 다 적어야 되는데 MyBatis-config.xml에서 typeAlias 에 추가해 줬다.    &ndash;&gt;-->
        <!--&lt;!&ndash;    이거 호출하려면 boardDao필요하다.&ndash;&gt;-->
        <!--&lt;!&ndash;    게시물 번호 주면(bno) 게시물 하나 읽어오는것&ndash;&gt;-->
        <!--    <select id="select" parameterType="int" resultType="BoardDto">-->
        <!--        <include refid="selectFromBoard"/>-->
        <!--        WHERE bno = #{bno}-->
        <!--    </select>-->

        <!--&lt;!&ndash;    페이징 값은 두개가 필요하므로 parameterType으로 map사용  &ndash;&gt;-->
        <!--    <select id="selectPage" parameterType="map" resultType="BoardDto">-->
        <!--        <include refid="selectFromBoard"/>-->
        <!--        ORDER BY reg_date DESC, bno DESC-->
        <!--        LIMIT #{offset}, #{pageSize}-->
        <!--    </select>-->

        <!--    <update id="update" parameterType="BoardDto">-->
        <!--        UPDATE board-->
        <!--        SET title   = #{title}-->
        <!--          , content = #{content}-->
        <!--          , up_date = now()-->
        <!--        WHERE bno = #{bno} and writer = #{writer}-->
        <!--    </update>-->

        <!--    <update id="updateCommentCnt" parameterType="map">-->
        <!--        UPDATE board-->
        <!--        SET comment_cnt = comment_cnt + #{cnt}-->
        <!--        WHERE bno = #{bno}-->
        <!--    </update>-->

        <!--&lt;!&ndash;    조회 했을 때 조회수 올리기&ndash;&gt;-->
        <!--    <update id="increaseViewCnt" parameterType="int">-->
        <!--        UPDATE board-->
        <!--        SET view_cnt = view_cnt + 1-->
        <!--        WHERE bno = #{bno}-->
        <!--    </update>-->

        <!--&lt;!&ndash;    검색&ndash;&gt;-->
        <!--    <sql id="searchCondition">-->
        <!--        <choose>-->
        <!--            <when test='option=="T"'>-->
        <!--                AND title LIKE concat('%', #{keyword}, '%')-->
        <!--            </when>-->
        <!--            <when test='option=="W"'>-->
        <!--                AND writer LIKE concat('%', #{keyword}, '%')-->
        <!--            </when>-->
        <!--            <otherwise>-->
        <!--                AND (title LIKE concat('%', #{keyword}, '%')-->
        <!--                OR content LIKE concat('%', #{keyword}, '%'))-->
        <!--            </otherwise>-->
        <!--        </choose>-->
        <!--    </sql>-->

        <!--    <select id="searchSelectPage" parameterType="SearchCondition" resultType="BoardDto">-->
        <!--        SELECT bno, title, content, writer, view_cnt, comment_cnt, reg_date-->
        <!--        FROM board-->
        <!--        WHERE true-->
        <!--        <include refid="searchCondition"/>-->
        <!--        ORDER BY reg_date DESC, bno DESC-->
        <!--        LIMIT #{offset}, #{pageSize}-->
        <!--    </select>-->

        <!--&lt;!&ndash;  cnt 검색결과가 몇개인지 알아야 페이징 할 수 있다.   &ndash;&gt;-->
        <!--    <select id="searchResultCnt" parameterType="SearchCondition" resultType="int">-->
        <!--        SELECT count(*)-->
        <!--        FROM board-->
        <!--        WHERE true-->
        <!--        <include refid="searchCondition"/>-->
        <!--    </select>-->

        <!--</mapper>-->